import './coin-world08-africa.js';
import {WORLD_BATCH09_ALL as AREAS,worldBatch09IssuerTerritoryStats as stats,worldBatch09IssuerTerritoryExcelRows as excelRows} from './world-batch09-issuers-territories-reference.mjs';
const norm=v=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();
function findArea(r){const hay=norm([r.country,r.currency,r.visibleText,r.obverseDesign,r.reverseDesign].join(' '));return AREAS.find(a=>[a.area,...a.aliases].some(x=>hay.includes(norm(x))))||null;}
const previous=window.coinReferenceMatcher;window.coinReferenceMatcher=r=>{r=previous?previous(r):r;const a=findArea(r);if(!a)return r;const set=(k,v)=>{if((r[k]==null||String(r[k]).trim()===''||String(r[k]).includes('Needs review'))&&v)r[k]=v};set('country',a.area);set('currency',a.currency);set('referenceAuthority',a.issuer);set('publicReferenceSource',a.source);set('researchSource1',a.source);set('informationStatus',a.status);set('researchPriority',a.kind.includes('collector')?'Interesting':'Possibly interesting');set('researchReason',a.note);set('publicReferenceMatch',`${a.area} — issuer/territory audit`);return r;};
window.coinWorld09IssuersTerritories={areas:AREAS,stats:stats()};
function addSheet(){if(!window.XLSX)return;const orig=window.XLSX.writeFile;if(orig.__issuerTerritoryWrapped)return;const wrapped=function(wb,...args){try{if(wb?.SheetNames&&!wb.SheetNames.includes('Issuers Territories'))window.XLSX.utils.book_append_sheet(wb,window.XLSX.utils.json_to_sheet(excelRows()),'Issuers Territories')}catch(e){console.warn('Issuer territory sheet:',e)}return orig.call(this,wb,...args)};wrapped.__issuerTerritoryWrapped=true;window.XLSX.writeFile=wrapped;}addSheet();setTimeout(addSheet,0);
const status=()=>{const el=document.querySelector('#reference-db-status');if(el){const s=stats();el.textContent=`Reference database active · sovereign world coverage + issuer/territory layer · ${s.areas} audited non-standard issuing/currency areas · ${s.verified} verified · ${s.baselineOfficial} official-baseline areas pending issue-level hardening.`}};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',status);else status();
