import test from 'node:test';import assert from 'node:assert/strict';import {normalizeCoinText,textSignalTokens,textSignalScore,referenceConfidence,pairingStatus} from '../beta/coin-quality.mjs';
test('normalizes visible coin text safely',()=>{assert.equal(normalizeCoinText('RÉPUBLIQUE  Française · 2001'),'republique francaise 2001');assert.deepEqual(textSignalTokens('2 EURO 2001 A'),['euro','2001']);});
test('visible text is supporting evidence, not a standalone identification',()=>{const s=textSignalScore({referenceClues:'REPUBLIQUE FRANCAISE'},{country:'France',currency:'Euro',type:'1 euro',design:'Tree République Française',catalogue:'French euro'});assert.ok(s.score>0);assert.ok(s.hits.includes('republique'));});
test('confidence rises when independent evidence agrees',()=>{const r={country:'Netherlands',currency:'Euro',denomination:'2 euro',year:'2014',weight:'8.5',diameter:'25.75',referenceClues:'Willem Alexander'};const hit={matched:true,ambiguous:false,country:'Netherlands',currency:'Euro',type:'Netherlands 2 euro Willem-Alexander',design:'King Willem-Alexander',catalogue:'Dutch euro',yearFrom:2014,yearTo:2026,weight:8.5,diameter:25.75,composition:'Bimetal'};const c=referenceConfidence(r,hit);assert.equal(c.level,'High');assert.ok(c.score>=.78);assert.ok(c.reasons.some(x=>x.includes('Year')));});
test('ambiguous sparse match stays below high confidence',()=>{const c=referenceConfidence({country:'Germany'},{matched:true,ambiguous:true,country:'Germany',type:'2 euro'});assert.notEqual(c.level,'High');});
test('pairing status explains automatic decision',()=>{assert.match(pairingStatus({pairs:[[0,0],[1,1]],confidence:'medium'}).message,/medium confidence/);assert.equal(pairingStatus(null).level,'none');});
